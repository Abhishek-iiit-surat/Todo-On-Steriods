const {logger} = require('../middlewares/logger');
const {createTask} = require('../models/taskModel')
const createTaskService = async (userId, title, description, due_date) => {
    // Logic to create a task in the database
    try {
    
    } catch (error) {
        
    }
}

module.exports = { createTaskService };